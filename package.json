{
  "name": "blogsite",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
  "install-a": "npm install --prefix back && npm install --prefix front", 
        "postinstall": "npm run install-a && npm run build",
        "build": "npm run build --prefix front",
        "dev": "concurrently \"npm run server\" \"npm run client\"",
        "server": "npm run dev --prefix back",
        "start": "npm run server",
        "client": "npm run dev --prefix front"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "concurrently": "^9.0.1"
  }
}
